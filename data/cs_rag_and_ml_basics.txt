# RAG 与机器学习相关概念（供 RAG）

检索增强生成先用检索模块从文档库取上下文，再将上下文与用户问题拼入提示调用大模型生成答案，减轻幻觉并提供出处。

向量检索将文档切块后嵌入为高维向量；查询同样嵌入后以最近邻等方法取 Top-K 片段拼接为上下文。

嵌入模型把文本映射到语义相近则距离较小的空间；需与向量库一致的维度与度量；国内可用兼容 OpenAI Embedding 的云服务接口。

小规模训练数据集易过拟合；交叉验证估算泛化；正则化与学习率调度常用来稳定训练曲线。
