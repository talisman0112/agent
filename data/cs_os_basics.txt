# 操作系统基础（供 RAG）

操作系统管理硬件资源并向应用提供抽象：典型功能含进程调度、内存管理、文件系统、设备驱动与安全策略。

进程是资源分配的单位，线程是调度单位；同一进程内的多线程共享地址空间；竞态时需互斥锁或信号量等同步手段。

分时调度让多程序看似并发；抢占式调度可被更高优先级或可运行任务打断非阻塞任务以提高响应。

虚拟内存为每个进程提供独立逻辑地址空间；通过页表将逻辑页映射到物理帧；页缺失时转入缺页中断由内核载入页。

分页与分段可结合；多级页表节省空间；写时复制常用于 fork 以降低内存占用。
