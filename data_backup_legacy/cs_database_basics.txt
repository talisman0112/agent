# 数据库基础（供 RAG）

关系数据库用表建模实体与联系；一行为一元组一列为一属性；键含主键外键以保持引用完整性。

Structured Query Language 用于定义与操纵数据：SELECT 投影与过滤 JOIN 联结聚合 GROUP BY。

事务是将若干操作视作原子单元的抽象；原子性一致性隔离性持久性合称 ACID；隔离级别在读未提交至可串行化。

索引加速查询但写入需维护额外结构；常见为 B 树或其变体；哈希索引对等值查询效率高。
