# 仅在需要入库或加载 Excel / 旧版 PPT 时再安装（叠加在 requirements.txt 之上）
#
#   pip install -r requirements.txt -r requirements-optional.txt
#
# unstructured 较重且依环境而异，按需参考：https://docs.unstructured.io/

unstructured>=0.14.0
openpyxl>=3.1.0
python-pptx>=0.6.21
